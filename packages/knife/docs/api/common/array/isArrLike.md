# isArrLike
```ts
/**
 * 判断对象是否是（类）数组
 * @category Array
 * @param o
 * @returns {boolean}
 */
export declare const isArrLike: <T>(o: any) => o is ArrayLike<T>

```

## Test
```ts
import { describe, test } from 'vitest'

describe('isArrLike', () => {
  test('isArrLike', () => {

  })
})
```
